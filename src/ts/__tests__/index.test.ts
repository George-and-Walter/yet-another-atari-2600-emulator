describe('Smoke Test', () => {
  it('should run', () => {
    expect(1).toBe(1);
  });
});

describe('someHandler', () => {

  // once upload is complete, pass rom into the cpu
  it('should read an instruction from the ROM', () => {

  });

  // read data from the rom file in a loop

  it('should do what the instruction from the ROM says', () => {
      // what does the html 5 file api provide as an input to other things

      someHandler()
  });
});
