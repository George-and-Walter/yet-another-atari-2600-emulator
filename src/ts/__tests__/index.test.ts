describe('Smoke Test', () => {
  it('should run', () => {
    expect(1).toBe(1);
  });
});

describe('', () => {
  it('should read an instruction from the ROM', () => {
  });
  it('should do what the instruction from the ROM says', () => {

  });
});
